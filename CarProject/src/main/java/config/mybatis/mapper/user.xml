<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
	
	<!-- u_id에 해당되는 1건 데이터 -->
	<select id="user_one_id" parameterType="int" resultType="vo.UserVo">
		select * from user_info where u_id=#{ u_id }
	</select>
	
	<select id="user_one_phone" parameterType="String" resultType="vo.UserVo">
		select * from user_info where u_name=#{ u_name } and u_phone=#{ u_phone } 
	</select>
	
	<select id="user_one_email" parameterType="String" resultType="vo.UserVo">
		select * from user_info where u_name=#{ u_name } and u_email=#{ u_email } 
	</select>
	
	<select id="user_one_pwd" parameterType="String" resultType="vo.UserVo">
		select * from user_info where u_id=#{ u_id } and u_name=#{ u_name } and u_phone=#{ u_phone } 
	</select>
	
	<update id="user_pwd_find" parameterType="String">
		update user_info set u_pwd=#{new_pwd}
	</update>
	
</mapper>




